<%= form_for [topic, post, post.comments.new] do |f| %>
  <h4> Add a Comment </h4>
    <%= f.text_area :body, rows: 6, class: 'form-control', placeholder: "Enter comment body" %>
  
  <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
<% end %>
